1️⃣ USER (someone else’s profile – username based)
GET    /users/<username>/
GET    /users/<username>/contact-info/

POST   /users/<username>/message/

POST   /users/<username>/follow/
POST   /users/<username>/unfollow/

POST   /users/<username>/block/
POST   /users/<username>/unblock/

GET    /users/<username>/posts/
GET    /users/<username>/followers/
GET    /users/<username>/following/

GET    /users/<username>/relationship-state/

2️⃣ PROFILE (your own profile – me + username)
GET    /me/<username>/
PATCH  /me/<username>/

GET    /me/<username>/contact-info/
PATCH  /me/<username>/contact-info/

GET    /me/<username>/posts/
GET    /me/<username>/comments/
GET    /me/<username>/reactions/
GET    /me/<username>/media/

👉 Redirect / Entry to analytics (analytics handled separately)
GET /me/<username>/analytics/

3️⃣ ANALYTICS (separate module, starts after entry)
GET /me/<username>/analytics/impressions/
GET /me/<username>/analytics/profile-views/
GET /me/<username>/analytics/post-engagement/
GET /me/<username>/analytics/followers-growth/

4️⃣ NETWORK / MYNETWORK (connections only)
GET  /network/

Requests
GET  /network/requests/received/
GET  /network/requests/sent/

Actions
POST /network/requests/<username>/accept/
POST /network/requests/<username>/ignore/

Lists
GET /network/connections/
GET /network/suggestions/
